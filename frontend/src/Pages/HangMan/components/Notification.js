import React from "react";

const Notification = () => {
  return (
    <div className="notification-container-show">
      <p>You have already entered this letter</p>
    </div>
  );
};

export default Notification;
{/* <div className={` ${showNotification ? "notification-container-show": false}`}> */}